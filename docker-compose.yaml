version: '2'

services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "9501:3002"
    volumes:
      - .:/code/build
      - .:/code/uploads
